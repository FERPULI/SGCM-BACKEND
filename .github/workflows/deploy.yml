name: Desplegar a HostinPeru 🚀

on:
  push:
    branches:
      - main  # ⚠️ Cambia esto si quieres desplegar desde 'rama-fernandez'

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Obtener el código más reciente
      uses: actions/checkout@v4

    # (OPCIONAL) Si usas React/Vue/Angular, descomenta estas líneas para construir:
    # - name: 🔧 Instalar dependencias y construir
    #   run: |
    #     npm install
    #     npm run build

    - name: 📂 Sincronizar archivos al Hosting
      uses: SamKirkland/FTP-Deploy-Action@v4.3.4
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        # Si usas React/Vue, cambia './' por './dist' o './build' abajo:
        local-dir: ./